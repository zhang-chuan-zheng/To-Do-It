# Default icons

本目录保存主页面默认 SVG 图标，稳定文件名为 `search.svg`、`minimize.svg`、`maximize.svg`、`restore.svg`、`close.svg`、`chevron-down.svg`、`calendar.svg`、`sort-up.svg`、`sort-down.svg`、`add.svg`、`attachment-add.svg` 和 `palette.svg`。

QML 通过 `IconCatalog.qml` 使用资源键；用户自定义版本应放在配置指定的自定义图标根目录并保持同名结构，不得写回本目录。
