# Default logos

本目录保存发行回退应用标志：`app.svg` 供 QML 标题栏和窗口图标使用，`app.ico` 通过 Windows RC 写入 `ToDoIt.exe`，并由 Qt IFW 用作安装器图标。

开发期自定义图标可分别放入 `assets/logos/custom/app.svg` 与 `assets/logos/custom/app.ico`；运行期用户自定义 Logo 应放在配置指定的自定义根目录并保持同名结构。任何自定义资源都不得覆盖本目录的发行回退文件。
